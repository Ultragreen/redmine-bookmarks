<h2><%=l('bookmarks.views.edit_link.title')%></h2>


<%- labelled_tabular_form_for :link, @link, :url => { :controller => 'bookmark', :action => "edit_link" }, :html => { :id => 'link-form' } do |f| -%>
	<%= render :partial => 'bookmark/form_link', :locals => { :f => f } %>
	<br>
	<%= submit_tag l('bookmarks.views.edit_button'), :name => "edit", :class => "small" %>
<% end %>